# Internal: Output dataframe

Internal Dataset: Empty output dataframe for metadata_map to fill.
Created by:  
  

1.  `output_df <- data.frame(timestamp = character(0), table = character(0), variable_n = character(0), variable = character(0), domain_code = character(0), note = character(0))`

2.  `usethis::use_data(output_df)`

## Usage

``` r
data(output_df)
```

## Format

A data frame with 0 rows and 6 columns

## Source

Dataframe was manually created as package data, using the above code.
